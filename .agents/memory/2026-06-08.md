# 2026-06-08

- Brainstormed a source-neutral summarization boundary.
- Approved direction: loaders return `ContentDocument`; localized system and
  user prompts remain under `locales/`; source content is sent as `source.md`
  when supported with a single inline Markdown fallback for identifiable
  unsupported-file errors.
- Web page extraction is out of scope for this change, but future web loaders
  will use the same `ContentDocument` contract.
- Saved the implementation sequence in
  `docs/2026-06-08-content-document-summarization/PLAN.md`.
- Implemented the approved direction on
  `feature/content-document-summarization`: `ContentDocument`, video loader and
  handler migration, localized system/user prompts, preferred `source.md`
  Chat Completions content, and narrow inline Markdown fallback.
- Extended every localized `openai.system_prompt` with summary-writing guidance
  derived from the agent-kit `SOUL.md`: direct and precise prose, restrained
  formatting, complexity-proportional length, and no unsupported claims.
